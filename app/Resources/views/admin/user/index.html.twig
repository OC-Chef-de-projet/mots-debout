{% extends 'base.html.twig' %}

{% block body %}

    <main id="admin">
        <div class="col container">
            <div class="card">
                <div class="card-image">
                    <h4 class="hdeader">Gestion des utilisateurs
                        <a href="{{ path('admin_user_new') }}" class="btn-floating btn-large waves-effect waves-light back-brown text-yellow right">
                            <i class="material-icons">add</i></a>
                    </h4>
                </div>

                <div class="card-stacked">

                    <div class="card-content card-section">


                        <table class="table table-condensed">
                            <thead>
                            <tr>
                                <th>Nom</th>
                                <th>Email</th>
                                <th>Rôle</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for user in users %}
                                <tr>
                                    <td><a href="{{ path('admin_user_edit', { 'id': user.id }) }}">{{ user.name }}</a>
                                    </td>
                                    <td>{{ user.email }}</td>
                                    <td>
                                        {{ user.rolesString }}
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
    </main>
{% endblock %}
